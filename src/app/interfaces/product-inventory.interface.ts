export interface ProductInventory {
  location: string,
  lotNumber: string,
  unit: string,
  quantity: number,
  packs: string,
  expDate: string,
  company: string,
  productId: string,
  productText: string,
  locationId: string,
  licensePlate: string
}
